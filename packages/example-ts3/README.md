# ts-overrides-plugin example

Before all:

```bash
yarn ts-patch install
```

Commands:

```bash
yarn build:tsc
yarn build:ts-loader
yarn build:fork-ts
yarn watch:tsc
yarn watch:ts-loader
yarn watch:fork-ts
```
